{
  "name": "flagfilter",
  "version": "1.1.0",
  "private": true,
  "description": "Cloudflare Pages app for browsing and filtering national flags",
  "scripts": {
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy . --project-name flagfilter"
  }
}
